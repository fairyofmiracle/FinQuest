# 🚀 Инструкция по установке FinQuest

## Быстрая установка

### 1. Клонирование и настройка окружения

```bash
# Клонировать проект
git clone <repository-url>
cd fin_project

# Создать виртуальное окружение
python -m venv venv

# Активировать виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### 2. Настройка базы данных

```bash
# Применить миграции
python manage.py migrate
```

### 3. Загрузка начальных данных

```bash
# Создать структуру тем, уровней и вопросов
python manage.py create_new_structure

# (Опционально) Загрузить статьи
python manage.py load_articles
```

⚠️ **ВАЖНО:** 
- Используйте `create_new_structure` вместо `load_fixtures` - она создает правильную структуру категорий!
- Команда `load_fixtures` устарела и может создать дубликаты - **НЕ используйте её**!

### 4. Создание администратора

```bash
# Создать суперпользователя интерактивно
python manage.py createsuperuser
```

**Или использовать готового администратора:**
- Логин: `admin`
- Пароль: `admin123`

⚠️ **ВАЖНО:** Измените пароль администратора в production!

### 5. Запуск сервера

```bash
python manage.py runserver
```

Откройте браузер: http://127.0.0.1:8000/

---

## 📋 Проверка установки

### Админ панель
http://127.0.0.1:8000/admin/
- Логин: `admin`
- Пароль: `admin123`

### Главная страница
http://127.0.0.1:8000/

### Регистрация нового пользователя
http://127.0.0.1:8000/accounts/register/

---

## 🎮 Первые шаги

1. **Зарегистрируйтесь** на сайте
2. **Выберите аватарку** в настройках профиля
3. **Пройдите обучение** (появится при первом входе)
4. **Начните изучать** финансовую грамотность!

---

## 📁 Структура проекта

```
fin_project/
├── accounts/          # Управление пользователями
├── game/              # Игровая механика
│   ├── fixtures/      # Начальные данные
│   ├── management/    # Команды управления
│   └── migrations/    # Миграции БД
├── static/            # Статические файлы (CSS, JS, изображения)
│   └── avatars/       # SVG аватарки по умолчанию
├── templates/         # HTML шаблоны
├── media/             # Загруженные пользователями файлы
├── db.sqlite3         # База данных SQLite
└── manage.py          # Управление Django
```

---

## 🔧 Дополнительные команды

### Создание ежедневных квестов
```bash
python manage.py create_daily_quests
```

### Сброс прогресса пользователя (для тестирования)
Доступно через веб-интерфейс в настройках профиля

### Запуск тестов
```bash
# Все тесты
python manage.py test

# Тесты безопасности
python manage.py test game.test_security

# Базовые тесты
python manage.py test game.tests
```

---

## 🛡️ Безопасность (для production)

Перед развертыванием на production:

1. **Измените SECRET_KEY** в `settings.py`
2. **Установите DEBUG = False**
3. **Настройте ALLOWED_HOSTS**
4. **Измените пароль администратора**
5. **Используйте PostgreSQL** вместо SQLite
6. **Настройте HTTPS**

---

## 📊 Что уже загружено

После выполнения `create_new_structure`:
- ✅ 4 основные категории
- ✅ 16 подкатегорий (тем)
- ✅ 48 уровней с разнообразными вопросами
- ✅ 2 достижения
- ✅ 4 SVG аватарки по умолчанию

**Категории:**
- 📚 Основы финансов (Бюджетирование, Сбережения, Кредиты, Банковские услуги)
- 📈 Инвестиции (Акции, Облигации, Недвижимость, Криптовалюты)
- 📋 Планирование (Пенсионный план, Налоги, Страхование, Финансовые цели)
- 🛡️ Безопасность (Мошенничество, Защита данных, Юридическая грамотность, Финансовые риски)

---

## ❓ Часто задаваемые вопросы

### Где находятся аватарки?
`static/avatars/default/` - 4 SVG файла (default1-4.svg)

### Как добавить новые уровни?
Через админ панель: http://127.0.0.1:8000/admin/game/level/

### Как добавить статьи?
Через админ панель: http://127.0.0.1:8000/admin/game/article/

### Как сбросить базу данных?
```bash
# Удалить БД (Windows)
del db.sqlite3
# Или Linux/Mac
# rm db.sqlite3

# Создать заново
python manage.py migrate
python manage.py create_new_structure
python manage.py createsuperuser
```

---

## 🆘 Решение проблем

### Ошибка "no such table"
```bash
python manage.py migrate
```

### Ошибка "MultipleObjectsReturned" при просмотре категорий
Это означает, что в базе есть дубликаты. Очистите и пересоздайте структуру:
```bash
python manage.py shell -c "from game.models import Topic, Level, LevelOption; Level.objects.all().delete(); LevelOption.objects.all().delete(); Topic.objects.all().delete()"
python manage.py create_new_structure
```

### Ошибка "ModuleNotFoundError"
```bash
pip install -r requirements.txt
```

### Порт 8000 занят
```bash
python manage.py runserver 8001
```

### Очистка кэша
```bash
# Удалить __pycache__
Get-ChildItem -Path . -Filter __pycache__ -Recurse -Directory | Remove-Item -Recurse -Force
```

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в терминале
2. Убедитесь, что все зависимости установлены
3. Проверьте, что миграции применены
4. Создайте Issue в репозитории

---

**FinQuest** - изучайте финансовую грамотность играючи! 🎮💰

