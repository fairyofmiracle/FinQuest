# 📱 Доступ к сайту с мобильного устройства

## Быстрый старт

### 1. Узнайте IP-адрес вашего компьютера

**Windows:**
```bash
ipconfig
```
Ищите **IPv4-адрес** в разделе **"Адаптер беспроводной локальной сети"** или **"Wireless LAN adapter"**

Пример: `192.168.1.100` или `192.168.0.5`

**Linux/Mac:**
```bash
ifconfig
# или
ip addr
```

### 2. Запустите сервер с доступом из сети

**Вариант 1: Используйте готовый скрипт (Windows)**
```bash
runserver_mobile.bat
```

**Вариант 2: Запустите вручную**
```bash
python manage.py runserver 0.0.0.0:8000
```

⚠️ **Важно:** Используйте `0.0.0.0:8000` вместо просто `8000` - это позволит серверу принимать подключения из сети.

### 3. Подключите телефон к той же Wi-Fi сети

Убедитесь, что ваш телефон подключен к **той же Wi-Fi сети**, что и компьютер.

### 4. Откройте сайт на телефоне

В браузере телефона откройте:
```
http://ВАШ_IP:8000
```

Например:
- `http://192.168.1.100:8000`
- `http://192.168.0.5:8000`

---

## 🔧 Решение проблем

### Не могу подключиться с телефона

1. **Проверьте, что телефон в той же Wi-Fi сети**
   - Убедитесь, что телефон подключен к той же сети, что и компьютер

2. **Проверьте файрвол**
   - Windows может блокировать входящие подключения
   - Разрешите Python через файрвол при запросе

3. **Проверьте IP-адрес**
   - Убедитесь, что используете правильный IP (не localhost, не 127.0.0.1)
   - IP должен начинаться с `192.168.` или `10.` (локальная сеть)

4. **Проверьте, что сервер запущен с 0.0.0.0**
   ```bash
   python manage.py runserver 0.0.0.0:8000
   ```
   Не используйте `127.0.0.1:8000` или просто `:8000`

### Ошибка "DisallowedHost"

Если видите ошибку про `ALLOWED_HOSTS`, убедитесь, что в `settings.py`:
```python
ALLOWED_HOSTS = ['*']  # для разработки
```

### Не работает на Android

- Попробуйте использовать Chrome вместо стандартного браузера
- Убедитесь, что используете `http://` (не `https://`)

### Не работает на iPhone

- Safari должен работать нормально
- Убедитесь, что используете `http://` (не `https://`)

---

## 🎯 Тестирование мобильной версии

После подключения с телефона:

1. Откройте главную страницу
2. Нажмите кнопку **"Мобильная версия"**
3. Сайт переключится в режим мобильного приложения (ширина 375px)

Или используйте кнопку переключения в навигации (для авторизованных пользователей).

---

## 🔒 Безопасность

⚠️ **Важно для production:**

- `ALLOWED_HOSTS = ['*']` - **только для разработки!**
- В production используйте конкретные домены:
  ```python
  ALLOWED_HOSTS = ['yourdomain.com', 'www.yourdomain.com']
  ```
- Не используйте `DEBUG = True` в production
- Используйте HTTPS

---

**Готово!** Теперь вы можете тестировать сайт на реальном мобильном устройстве! 📱✨

