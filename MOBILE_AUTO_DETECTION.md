# 📱 Автоматическое определение мобильных устройств

## ✅ Что уже работает:

### 🔄 Автоматическое определение

Приложение **автоматически определяет** тип устройства и показывает соответствующую версию:

#### 💻 На компьютере (Desktop):
- Chrome, Firefox, Edge, Safari (macOS)
- **→ Открывается веб-версия** (с обычным меню, sidebar, и т.д.)

#### 📱 На мобильном устройстве:
- iPhone (Safari, Chrome)
- iPad
- Android (Chrome, Samsung Internet, Firefox)
- **→ Открывается мобильная версия** (с нижней навигацией, компактный дизайн)

---

## 🎯 Как это работает?

### Автоматическое определение по User-Agent:

Файл: `game/context_processors.py`

```python
def mobile_view_context(request):
    """Автоматически определяет мобильное устройство по User-Agent"""
    user_agent = request.META.get('HTTP_USER_AGENT', '').lower()
    
    # Список ключевых слов для определения мобильных устройств
    mobile_keywords = [
        'mobile', 'android', 'iphone', 'ipod', 'ipad', 
        'blackberry', 'windows phone', 'webos', 'opera mini',
        'iemobile', 'mobile safari'
    ]
    
    # Проверяем есть ли мобильные ключевые слова в User-Agent
    is_mobile = any(keyword in user_agent for keyword in mobile_keywords)
    
    return {
        'mobile_view': is_mobile
    }
```

Эта функция проверяет User-Agent браузера и определяет мобильное устройство.

---

## 🔧 Ручное переключение (для тестирования)

### На компьютере:

Если хотите **протестировать мобильную версию на компьютере**, есть два способа:

#### Способ 1: Режим разработчика браузера (DevTools)

1. Откройте сайт в браузере
2. Нажмите **F12** (открыть DevTools)
3. Нажмите **Ctrl+Shift+M** (или Cmd+Shift+M на Mac)
4. Выберите устройство: iPhone 12 Pro, Pixel 5, и т.д.
5. Обновите страницу (**F5**)
6. **→ Откроется мобильная версия!** 📱

#### Способ 2: Кнопка переключения в меню

1. Войдите в аккаунт
2. В десктопном меню справа вверху есть кнопка: 📱
3. Нажмите на неё
4. **→ Переключится на мобильную версию!**
5. Чтобы вернуться: откройте гамбургер-меню ☰ → "Перейти на Desktop"

### На телефоне:

Если хотите **открыть веб-версию на телефоне**:

1. Откройте гамбургер-меню ☰ (справа вверху)
2. Нажмите "Перейти на Desktop" 🖥️
3. **→ Переключится на веб-версию!**
4. Чтобы вернуться: нажмите кнопку 📱 в меню

---

## 🧪 Тестирование

### Проверка автоопределения:

```bash
# Запустите сервер
python manage.py runserver
```

Откройте в браузере:
```
http://127.0.0.1:8000
```

#### Тест 1: Обычный браузер (Desktop)
- Просто откройте сайт
- **Ожидаемый результат**: Веб-версия

#### Тест 2: Мобильный режим DevTools
- F12 → Ctrl+Shift+M
- Выберите iPhone 12 Pro
- Обновите страницу (F5)
- **Ожидаемый результат**: Мобильная версия

#### Тест 3: Реальный телефон
- Откройте на телефоне (в той же WiFi сети)
- Узнайте IP компьютера: `ipconfig` (Windows) или `ifconfig` (Mac/Linux)
- Откройте на телефоне: `http://ваш-ip:8000`
- **Ожидаемый результат**: Мобильная версия

---

## 🎨 Как понять какая версия открылась?

### Веб-версия (Desktop):
- ✅ Логотип "FinQuest" слева вверху
- ✅ Обычное меню справа (Статьи, Уведомления, Профиль)
- ✅ Широкие карточки
- ✅ Sidebar (если есть)
- ✅ Нет нижней навигации

### Мобильная версия:
- ✅ Название страницы в центре вверху
- ✅ Гамбургер-меню ☰ справа вверху
- ✅ **Нижняя навигация** с 6 кнопками
- ✅ Компактные карточки
- ✅ Градиенты и красивые эффекты
- ✅ Свайп-жесты (если включены)

---

## 🔍 Отладка

Если что-то не работает:

### Проверка 1: User-Agent определяется правильно

В Django shell:

```python
python manage.py shell

from django.test import RequestFactory
request = RequestFactory().get('/')
request.META['HTTP_USER_AGENT'] = 'Mozilla/5.0 (iPhone; CPU iPhone OS 15_0 like Mac OS X)'

from game.context_processors import mobile_view_context
print(mobile_view_context(request))
# Должно вывести: {'mobile_view': True}
```

### Проверка 2: Context processor подключен

Файл: `finquest/settings.py`

```python
TEMPLATES = [
    {
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                'game.context_processors.notifications_context',
                'game.context_processors.mobile_view_context',  # ← должен быть!
                'game.context_processors.achievements_context',
            ],
        },
    },
]
```

### Проверка 3: Шаблоны используют mobile_view

Файл: `templates/base.html`

```django
{% if mobile_view %}
  <!-- Мобильная версия -->
{% else %}
  <!-- Веб версия -->
{% endif %}
```

---

## 🚀 Деплой на сервер

После деплоя на сервер (см. `SSH_CONNECTION.md`), автоопределение будет работать **автоматически**:

1. Пользователи с компьютера → веб-версия
2. Пользователи с телефона → мобильная версия
3. Никаких дополнительных настроек не нужно!

---

## 📊 Какие устройства определяются как мобильные?

### ✅ Мобильные устройства (mobile_view = True):
- iPhone (все модели)
- iPad (все модели)
- Android телефоны и планшеты
- Windows Phone
- BlackBerry
- WebOS устройства
- Opera Mini
- Любой браузер с "mobile" в User-Agent

### ❌ НЕ мобильные (mobile_view = False):
- Windows (Chrome, Firefox, Edge)
- macOS (Safari, Chrome, Firefox)
- Linux (все браузеры)
- Chromebooks
- Любой desktop браузер

---

## 🎯 Принудительная установка версии (для тестирования)

Если нужно **принудительно** включить/выключить мобильную версию:

### Через URL:

```
http://127.0.0.1:8000/toggle-mobile/
```

Эта ссылка переключает режим и сохраняет выбор в сессии.

### Через код (для отладки):

```python
# В любой view можно установить
request.session['force_mobile_view'] = True  # Всегда мобильная
request.session['force_mobile_view'] = False  # Всегда веб
```

---

## ✅ Итог

**Всё работает автоматически!**

- ✅ На компьютере → веб-версия
- ✅ На телефоне → мобильная версия
- ✅ Можно переключать вручную (для тестирования)
- ✅ Выбор сохраняется в сессии

**Ничего дополнительного настраивать не нужно!** 🎉

---

**Создано для IT-Sprint 2025**  
**Команда: "Джунцы"**  
**Проект: FinQuest**

